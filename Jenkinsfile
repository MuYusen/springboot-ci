@Library('jenkins-shared-library-cicd')_

pipeline{
    agent any

    stages{
        stage("download_code"){
            steps{
                echo "download"
            }
        }
        stage("build"){
            steps{
                echo "build"
            }
        }
        stage("test"){
            steps{
                echo "test"
            }
        }
        stage("deploy"){
            steps{
                echo "deploy"
            }
        }
    }
}
